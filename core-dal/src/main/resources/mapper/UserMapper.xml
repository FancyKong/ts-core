<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jdkcc.ts.dal.mapper.UserMapper">
    <resultMap id="BaseResultMap" type="com.jdkcc.ts.dal.entity.User">
        <id     column="id"             property="id"               jdbcType="BIGINT"/>
        <result column="username"       property="username"         jdbcType="VARCHAR"/>
        <result column="password"       property="password"         jdbcType="VARCHAR"/>
        <result column="nickname"       property="nickname"         jdbcType="VARCHAR"/>
        <result column="telephone"      property="telephone"        jdbcType="VARCHAR"/>
        <result column="created_time"   property="createdTime"      jdbcType="TIMESTAMP"/>
        <result column="modified_time"  property="modifiedTime"     jdbcType="TIMESTAMP"/>
        <result column="is_active"      property="active"           jdbcType="INTEGER"/>
    </resultMap>
    <sql id="Base_Column_List">
        id,
        username,
        password,
        nickname,
        telephone,
        created_time,
        modified_time,
        is_active
    </sql>

    <insert id="insert" parameterType="com.jdkcc.ts.dal.entity.User">
        INSERT INTO t_user (
        <include refid="Base_Column_List"/>
        )
        VALUES (
        0,
        #{username,jdbcType=VARCHAR},
        #{password,jdbcType=VARCHAR},
        #{nickname,jdbcType=VARCHAR},
        #{telephone,jdbcType=VARCHAR},
        #{createdTime,jdbcType=TIMESTAMP},
        #{modifiedTime,jdbcType=TIMESTAMP},
        #{active,jdbcType=INTEGER})
    </insert>

    <update id="update" parameterType="com.jdkcc.ts.dal.entity.User">
        INSERT INTO t_user (
        <include refid="Base_Column_List"/>
        )
        VALUES (
        #{id,jdbcType=BIGINT},
        #{username,jdbcType=VARCHAR},
        #{password,jdbcType=VARCHAR},
        #{nickname,jdbcType=VARCHAR},
        #{telephone,jdbcType=VARCHAR},
        #{createdTime,jdbcType=TIMESTAMP},
        #{modifiedTime,jdbcType=TIMESTAMP},
        #{active,jdbcType=INTEGER})
        WHERE
        id = #{id,jdbcType=BIGINT}
    </update>

    <select id="findOne" resultMap="BaseResultMap" parameterType="java.lang.Long">
        SELECT
        <include refid="Base_Column_List"/>
        FROM t_user WHERE id = #{id,jdbcType=BIGINT}
    </select>

    <select id="count" resultMap="java.lang.Long" parameterType="java.lang.Long">
        SELECT count(*) FROM t_user
    </select>

    <delete id="delete" parameterType="java.lang.Long">
        DELETE  FROM t_user WHERE id = #{id,jdbcType=BIGINT}
    </delete>

    <select id="findAll" resultMap="BaseResultMap" >
        SELECT
        <include refid="Base_Column_List"/>
        FROM t_user
        limit #{0,jdbcType=INTEGER},#{1,jdbcType=INTEGER}
        ORDER BY id
    </select>

    <select id="findByUsername" resultMap="BaseResultMap" parameterType="java.lang.String">
        SELECT
        <include refid="Base_Column_List"/>
        FROM t_user WHERE username = #{username,jdbcType=VARCHAR}
    </select>

</mapper>